import { CursorEffectDirective } from './cursor-effect.directive';

describe('CursorEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorEffectDirective();
    expect(directive).toBeTruthy();
  });
});
